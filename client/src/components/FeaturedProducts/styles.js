import styled from "styled-components";

import { v } from "../../styles/variables";

export const SFeaturedProducts = styled.div`
    margin: ${v.lgSpacing} 0;
`;

export const SFeaturedProductsTitle = styled.span`
    display: block;
    font-size: 24px;
    font-weight: 500;
    text-align: center;
    margin-bottom: ${v.xlSpacing};
`;

export const SCTAContainer = styled.div`
    margin-top: ${v.lgSpacing};
    display: flex;
    justify-content: center;
`;
