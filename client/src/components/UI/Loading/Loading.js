import React from "react";
import { SLoading, SSpinner } from "./styles";

const Loading = () => {
    return (
        <SLoading>
            <SSpinner />
        </SLoading>
    );
};

export default Loading;
