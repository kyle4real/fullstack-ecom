import styled from "styled-components";

import { v } from "../../../styles/variables";

export const SResourceBottomBar = styled.div`
    border-top: 1px solid ${({ theme }) => theme.bg3};
    margin: ${v.mdSpacing} 0;
    padding: ${v.mdSpacing} 0;
`;
